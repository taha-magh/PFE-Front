import { SaerchAssociationDTO } from './saerch-association-dto';

describe('SaerchAssociationDTO', () => {
  it('should create an instance', () => {
    expect(new SaerchAssociationDTO()).toBeTruthy();
  });
});
